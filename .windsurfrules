# GymRatPlanner Windsurf rule pack has moved

The rules previously defined here now live in structured Windsurf rule files under `./.windsurf/rules`. Please edit those Markdown files directly.

Current rule files:

- `shared.mdc`
- `frontend.mdc`
- `react.mdc`
- `astro.mdc`
- `backend.mdc`
- `devops.mdc`

Each file begins with front-matter and applies to specific globs, mirroring the `.cursor/rules` layout.

